document.addEventListener('DOMContentLoaded',async()=>{const slot=document.getElementById('recountixAdSlot');if(!slot||typeof sbGetActiveAds!=='function')return;try{const ads=await sbGetActiveAds(currentShopId());if(!ads.length)return;const ad=ads[Math.floor(Math.random()*ads.length)];const card=document.createElement(ad.link_url?'a':'div');card.className='rx-ad-card';if(ad.link_url){card.href=ad.link_url;card.target='_blank';card.rel='noopener sponsored';card.addEventListener('click',()=>sbTrackAdClick(ad.id));}const copy=document.createElement('div');copy.className='rx-ad-copy';const label=document.createElement('span');label.className='rx-ad-label';label.textContent='Sponsored';const title=document.createElement('div');title.className='rx-ad-title';title.textContent=ad.title||'';const text=document.createElement('p');text.className='rx-ad-text';text.textContent=ad.description||'';copy.append(label,title,text);card.append(copy);if(ad.image_url){const img=document.createElement('img');img.className='rx-ad-image';img.src=ad.image_url;img.alt='';card.append(img);}if(ad.link_url){const cta=document.createElement('span');cta.className='rx-ad-cta';cta.textContent=ad.cta_text||'Learn More';card.append(cta);}slot.append(card);slot.hidden=false;}catch(e){console.warn('Ad banner',e);}});